pre-commit:
  commands:
    fix:
      glob: "{src,netlify/functions}/**/*.{ts,tsx,js,jsx}"
      run: "pnpm run lint"
      stage_fixed: true
    test:
      run: "pnpm run test"
    typecheck:
      run: "pnpm run typecheck"
