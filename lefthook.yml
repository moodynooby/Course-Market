pre-commit:
  commands:
    fix:
      glob: "{src,netlify/functions}/**/*.{ts,tsx,js,jsx}"
      run: "bun run fix"
      stage_fixed: true
    test:
      run: "bun run test:run"
    typecheck:
      run: "bun run tsc -b"
