## 2025-05-15 - [Dynamic Tooltips for Toggle States]
**Learning:** For stateful icon buttons (like expand/collapse), dynamic tooltips that reflect the *action* (e.g., "Show sections") rather than the *state* provide better immediate clarity and accessibility.
**Action:** Always pair stateful icon buttons with dynamic `aria-label` and `Tooltip` that update based on the current state.
